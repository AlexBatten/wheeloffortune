package com.example.wheeloffortune.model

import androidx.compose.ui.graphics.Color


class Field (point: Int, bankrupt: Boolean, color: Color) {
    val point: Int = point
    val bankrupt: Boolean = bankrupt
    val color: Color = color
}