package com.example.wheeloffortune.model

import androidx.compose.ui.graphics.Color


class Field (point: Int, bankrupt: Boolean, color: Color) {
    private val point: Int = point
    private val bankrupt: Boolean = bankrupt
    private val color: Color = color
}